$(document).ready(function(){
    // Remove Magnific Popup initialization

    // Remove click event for project buttons
    $(".project-btn").click(function(e) {
        // You might not need this anymore
        // e.stopPropagation(); // Prevent any default behavior
    });
});
